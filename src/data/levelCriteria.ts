// 5단계(관심~심각) 판정을 위한 규칙표.
// 근거: docs/재난유형별_단계구분_기준.md — 한국은 "기상특보(주의보/경보/중대경보)"와
// "위기경보(관심/주의/경계/심각)"라는 두 개의 독립된 체계를 쓰며, 재난마다 정량기준이 제각각이라
// 전국 단일 앱에서 실시간으로 정밀 재현하기는 불가능하다(문서 자체가 이를 명시).
// 따라서 이 앱은 "기상특보 단계·재난문자 등급"을 다음과 같이 5단계로 단순 매핑하는
// 보수적 근사 규칙을 쓴다(설계서 5.3과 동일한 성격의 근사).
//   - 기상특보: 예비특보→관심(2), 주의보→경계(4), 경보/중대경보→심각(5)
//   - 재난문자: 안전안내문자→관심(2), 재난문자→주의(3), 긴급재난문자→경계(4), 위급재난문자→심각(5)
// 여러 신호가 동시에 있으면 그 중 최댓값을 취하고, 아무 신호도 없으면 정상(1)이다.

import type { AlertLevel, DisasterLevel, MessageType } from "@/types";

export const WEATHER_ALERT_LEVEL: Record<AlertLevel, DisasterLevel> = {
  예비특보: 2,
  주의보: 4,
  경보: 5,
  중대경보: 5,
};

export const MESSAGE_TYPE_LEVEL: Record<MessageType, DisasterLevel> = {
  안전안내문자: 2,
  재난문자: 3,
  "재난문자(속보)": 3,
  긴급재난문자: 4,
  위급재난문자: 5,
};

export const LEVEL_REASONING_TEMPLATE: Record<DisasterLevel, string> = {
  1: "현재 발효 중인 기상특보나 재난문자가 없어 정상 상태로 판단했습니다.",
  2: "관심 수준의 특보 또는 안내문자가 확인되어 관심 단계로 판단했습니다.",
  3: "주의가 필요한 재난문자가 확인되어 주의 단계로 판단했습니다.",
  4: "특보 주의보 또는 긴급재난문자가 확인되어 경계 단계로 판단했습니다.",
  5: "특보 경보 이상 또는 위급재난문자가 확인되어 심각 단계로 판단했습니다.",
};
